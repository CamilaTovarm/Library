@model IEnumerable<BookHive.Models.ReturnsViewModel>

@{
    ViewData["Title"] = "Listado de Devoluciones";
}

<h1 class="page-title">@ViewData["Title"]</h1>

@if (TempData["errorMessage"] != null)
{
    <div class="alert alert-danger">@TempData["errorMessage"]</div>
}

@if (!Model.Any())
{
    <p>No hay devoluciones registradas.</p>
}
else
{
    <table class="table table-striped table-bordered">
        <thead class="table-success">
            <tr>
                <th>ID Devolución</th>
                <th>Usuario</th>
                <th>Libro</th>
                <th>Fecha de Devolución</th>
                <th>Multa Impuesta</th>
        </thead>
        <tbody>
            @foreach (var ret in Model)
            {
                <tr>
                    <td>@ret.ReturnId</td>
                    <td>@ret.UserName</td>
                    <td>@ret.BookTitle</td>
                    <td>@ret.ReturnDate.ToString("yyyy-MM-dd")</td>
                    <td>@ret.FineImposed.ToString("C")</td>
                </tr>
            }
        </tbody>
    </table>
}

<style>
    .page-title {
        color: #1b5e20;
        margin-top: 30px;
        margin-bottom: 20px;
        font-weight: 700;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    table.table {
        width: 100%;
        border-collapse: collapse;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

        table.table th, table.table td {
            padding: 12px 15px;
            border: 1px solid #ddd;
            text-align: left;
        }

        table.table thead {
            background-color: #43a047;
            color: white;
        }

        table.table tbody tr:hover {
            background-color: #f1f8f1;
        }

    .alert {
        font-weight: 600;
        margin-bottom: 20px;
    }
</style>
